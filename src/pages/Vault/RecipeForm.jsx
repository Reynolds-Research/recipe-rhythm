/**
 * RecipeForm — the add-recipe form. Owns its own draft state and the in-flight
 * AI-suggest / image-upload preview lifecycle. On submit it calls back into
 * the parent which dispatches to useVault's addRecipe action.
 *
 * Extracted from Vault.jsx in PRD-001 P0.9 (Phase 3 Step 2). Behavior is
 * bit-identical to the pre-split version.
 */

// implementation moves in a later commit
export default function RecipeForm() {
  return null
}
